	<div class="well">
	<span class="task">
		<%= link_to fa_icon('square-o', text: user_task.description), user_task, remote: true, method: :delete, data: { confirm: 'Are you sure?' }, class: 'task' %>
	</span>
	<%# TODO: make them buttons work. %>

        <%= link_to fa_icon('pencil', text: 'Edit'), edit_user_task_path(user_task), remote: true, class: 'btn btn-xs btn-primary' %>
 
        <span class="pull-right">
        	<em>
        		<%= user_task.due.try(:stamp, 'Monday, 12/31/16') %>
        	</em>
        </span>
    </div>